{
  "name": "reflector",
  "version": "1.0.0",
  "private": true,
  "description": "Live D-STAR reflector activity dashboard — Cloudflare Worker",
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.60.0"
  }
}

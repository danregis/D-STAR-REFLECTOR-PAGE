{
  "name": "dstar-reflector-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Live D-STAR reflector activity dashboard — Cloudflare Pages",
  "scripts": {
    "dev":    "wrangler pages dev public --compatibility-date=2024-06-01",
    "deploy": "wrangler pages deploy public --project-name=reflector"
  },
  "devDependencies": {
    "wrangler": "^3.60.0"
  }
}
